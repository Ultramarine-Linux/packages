name: Bootstrap
permissions:
  contents: read
on:
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        version: ["rawhide"]
        arch: ["x86_64", "aarch64"]
      fail-fast: false
    runs-on: ${{ matrix.arch == 'aarch64' && 'ubuntu-22.04-arm' || 'ubuntu-latest' }}
    container:
      image: ghcr.io/ultramarine-linux/builder:umrawhide
      options: --cap-add=SYS_ADMIN --privileged
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install build dependencies
        run: dnf builddep -y ultramarine/ultramarine-mock-configs*.spec

      - name: Build ultramarine-mock-configs
        run: |
          anda build -rrpmbuild -D "vendor Ultramarine Linux" ultramarine/ultramarine-mock-configs/pkg

      - name: Install ultramarine-mock-configs
        run: |
          dnf install -y anda-build/rpm/rpms/ultramarine-mock-configs*.rpm

      - name: Build ultramarine-release
        run: |
          anda build -D "vendor Ultramarine Linux" -c ultramarine-${{ matrix.version }}-${{ matrix.pkg.arch }} ultramarine/release/pkg

      - name: Upload packages to subatomic
        run: |
          subatomic-cli upload --prune \
            --server https://subatomic.fyralabs.com \
            --token ${{ secrets.SUBATOMIC_TOKEN }} \
            um${{ matrix.version }} anda-build/rpm/rpms/*

      - name: Upload source packages to subatomic
        run: |
          subatomic-cli upload --prune \
          --server https://subatomic.fyralabs.com \
          --token ${{ secrets.SUBATOMIC_TOKEN }} \
          um${{ matrix.version }}-source anda-build/rpm/srpm/*
